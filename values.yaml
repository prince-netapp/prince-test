provider: azure
policy: sync
commonLabels:
  azure.workload.identity/use: "true"
serviceAccount:
  create: true
  name: external-dns
  labels:
    azure.workload.identity/use: "true"
azure:
  secretName: external-dns-azure
